name: smoke
on:
  push:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from $(hostname) at $(date -u)"
      - run: |
          echo "Runner OS: $(uname -srm)"
          echo "Working dir: $(pwd)"
          echo "Smoke test passed."
